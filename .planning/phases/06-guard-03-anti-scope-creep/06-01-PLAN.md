---
phase: 06-guard-03-anti-scope-creep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/gg/.claude/CLAUDE.md
  - agents/gsd-executor.md
  - agents/gsd-planner.md
autonomous: true
requirements:
  - GUARD-03

must_haves:
  truths:
    - "CLAUDE.md contains a dedicated anti-scope-creep section with rules preventing feature additions beyond the plan"
    - "Executor agent has an XML block reinforcing scope boundaries during task execution"
    - "Planner agent has an XML block preventing over-scoping during plan creation"
  artifacts:
    - path: "/Users/gg/.claude/CLAUDE.md"
      provides: "Global anti-scope-creep guardrail"
      contains: "Anti-Scope-Creep"
    - path: "agents/gsd-executor.md"
      provides: "Executor-level scope enforcement XML block"
      contains: "<anti_scope_creep>"
    - path: "agents/gsd-planner.md"
      provides: "Planner-level over-scoping prevention XML block"
      contains: "<anti_scope_creep>"
  key_links:
    - from: "/Users/gg/.claude/CLAUDE.md"
      to: "all Claude Code sessions"
      via: "global instructions loaded at session start"
      pattern: "Anti-Scope-Creep"
    - from: "agents/gsd-executor.md"
      to: "plan execution"
      via: "executor agent prompt loaded during execute-phase"
      pattern: "anti_scope_creep"
    - from: "agents/gsd-planner.md"
      to: "plan creation"
      via: "planner agent prompt loaded during plan-phase"
      pattern: "anti_scope_creep"
---

<objective>
Implement GUARD-03 (anti-scope-creep) across the three-layer guardrail pattern established in Phase 3: global CLAUDE.md rule + executor agent XML block + planner agent XML block.

Purpose: Prevent executors from adding features beyond the plan and planners from over-scoping. This is the last unsatisfied requirement blocking the milestone.
Output: Three files patched with anti-scope-creep guardrails.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add anti-scope-creep section to CLAUDE.md global and executor agent</name>
  <files>/Users/gg/.claude/CLAUDE.md, agents/gsd-executor.md</files>
  <read_first>/Users/gg/.claude/CLAUDE.md, agents/gsd-executor.md</read_first>
  <action>
**CLAUDE.md** — Add a new `## Anti-Scope-Creep` section after the existing `## Causa Raiz` section (around line 33). Insert these exact rules:

```
## Anti-Scope-Creep
- Nao adicione features, melhorias ou refactorings alem do que o plano pede. O plano eh o escopo.
- Se perceber oportunidade de melhoria fora do escopo, registre em `deferred-items.md` no diretorio da fase — nao implemente.
- 3 linhas de codigo > abstracao prematura. Prefira codigo inline simples a criar helpers, utils ou abstractions que o plano nao pediu.
- Nao "melhore" codigo existente que nao faz parte do plano. Read-only para contexto, write-only para o que o plano pede.
- Se o plano diz "criar endpoint X", crie endpoint X. Nao crie tambem middleware generico, error handler universal, ou validation library.
```

**agents/gsd-executor.md** — Add an `<anti_scope_creep>` XML block immediately AFTER the existing `<anti_false_claims>` closing tag (which is around line 232). Insert:

```xml
<anti_scope_creep>
**The plan is the scope. Nothing more.**

- Before writing ANY code, ask: "Is this in the plan?" If not, don't write it.
- Do NOT add helper functions, utility modules, or abstractions the plan didn't request.
- Do NOT refactor existing code you encounter while implementing plan tasks.
- Do NOT add error handling, validation, or logging beyond what the task action specifies.
- 3 lines of inline code is ALWAYS better than a premature abstraction the plan didn't ask for.
- If you discover something that SHOULD be done but ISN'T in the plan, log it to `deferred-items.md` in the phase directory. Format: `- [task-name]: description of deferred item`.
- The declare_scope step at the start is your contract. Everything you do must trace back to it.

**Violation signals (STOP and remove):**
- Creating a new file the plan didn't list in `<files>`
- Adding exports/functions not mentioned in `<action>`
- "While I'm here, I'll also..." — NO. You won't.
- "This would be better if..." — Log it, don't do it.
</anti_scope_creep>
```
  </action>
  <verify>
    <automated>grep -c "Anti-Scope-Creep" /Users/gg/.claude/CLAUDE.md && grep -c "anti_scope_creep" /Volumes/SSD/Desenvolvimento/get-shit-done-grg/agents/gsd-executor.md</automated>
  </verify>
  <acceptance_criteria>
    - grep "Anti-Scope-Creep" /Users/gg/.claude/CLAUDE.md returns match
    - grep "anti_scope_creep" agents/gsd-executor.md returns match
    - grep "3 linhas de codigo" /Users/gg/.claude/CLAUDE.md returns match
    - grep "The plan is the scope" agents/gsd-executor.md returns match
    - grep "deferred-items.md" agents/gsd-executor.md returns match
    - grep "While I'm here" agents/gsd-executor.md returns match
    - Existing sections (Etica, Preservacao, Causa Raiz, anti_false_claims, context_persistence) remain intact
  </acceptance_criteria>
  <done>CLAUDE.md has Anti-Scope-Creep section with 5 rules. Executor agent has anti_scope_creep XML block with behavioral rules and violation signals.</done>
</task>

<task type="auto">
  <name>Task 2: Add anti-scope-creep block to planner agent</name>
  <files>agents/gsd-planner.md</files>
  <read_first>agents/gsd-planner.md</read_first>
  <action>
**agents/gsd-planner.md** — Add an `<anti_scope_creep>` XML block immediately AFTER the existing `<context_persistence>` closing tag (the last XML block in the agent file). Insert:

```xml
<anti_scope_creep>
**Plan what the phase requires. Nothing more.**

- Every task MUST trace to a requirement ID in the ROADMAP. No orphan tasks.
- Do NOT add "nice to have" tasks that aren't in the phase requirements.
- Do NOT add integration tests, documentation, or cleanup tasks unless the requirement explicitly calls for them.
- If a phase has 1 requirement, it likely needs 1 plan with 2-3 tasks — not 3 plans with 9 tasks.
- Resist the urge to "set up for the future" — plan for THIS phase's deliverables only.
- 3 lines > premature abstraction applies to plans too: a focused 2-task plan beats an elaborate 5-task plan that delivers the same outcome.
- If you find yourself adding tasks that start with "prepare", "scaffold", or "set up infrastructure for future..." — STOP. That's scope creep.

**Self-check before finalizing:**
- [ ] Every task traces to a phase requirement
- [ ] No task exists "just in case" or "for completeness"
- [ ] Plan count matches actual complexity, not perceived thoroughness
</anti_scope_creep>
```
  </action>
  <verify>
    <automated>grep -c "anti_scope_creep" /Volumes/SSD/Desenvolvimento/get-shit-done-grg/agents/gsd-planner.md</automated>
  </verify>
  <acceptance_criteria>
    - grep "anti_scope_creep" agents/gsd-planner.md returns match (at least 2: open and close tag)
    - grep "Plan what the phase requires" agents/gsd-planner.md returns match
    - grep "No orphan tasks" agents/gsd-planner.md returns match
    - grep "3 lines > premature abstraction" agents/gsd-planner.md returns match
    - Existing context_persistence and scope_estimation sections remain intact
  </acceptance_criteria>
  <done>Planner agent has anti_scope_creep XML block preventing over-scoping with self-check checklist.</done>
</task>

</tasks>

<verification>
All three layers of GUARD-03 anti-scope-creep guardrails are in place:
1. `grep "Anti-Scope-Creep" /Users/gg/.claude/CLAUDE.md` — global rule exists
2. `grep "anti_scope_creep" agents/gsd-executor.md` — executor enforcement exists
3. `grep "anti_scope_creep" agents/gsd-planner.md` — planner enforcement exists
4. `grep "deferred-items.md" agents/gsd-executor.md` — escape hatch for out-of-scope discoveries exists
5. `grep "3 lin" /Users/gg/.claude/CLAUDE.md` — "3 lines > premature abstraction" rule present
</verification>

<success_criteria>
- CLAUDE.md global has Anti-Scope-Creep section with rules preventing feature additions beyond the plan
- Executor agent has anti_scope_creep XML block with violation signals
- Planner agent has anti_scope_creep XML block with self-check checklist
- All three files retain their existing guardrails from Phase 3 (anti_false_claims, context_persistence, etc.)
- GUARD-03 requirement fully satisfied: "Don't add features beyond the plan, 3 lines > premature abstraction"
</success_criteria>

<output>
After completion, create `.planning/phases/06-guard-03-anti-scope-creep/06-01-SUMMARY.md`
</output>
