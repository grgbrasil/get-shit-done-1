---
phase: 03-model-routing-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - sdk/src/types.ts
  - sdk/src/context-engine.ts
  - sdk/src/context-engine.test.ts
  - get-shit-done/bin/lib/init.cjs
  - get-shit-done/bin/lib/fmap.cjs
autonomous: true
requirements: [INT-02]
must_haves:
  truths:
    - "Context Engine resolves functionMapStats field when .planning/function-map-stats.json exists"
    - "Context Engine returns undefined for functionMapStats when stats file does not exist"
    - "Init command for execute-phase writes function-map-stats.json when function-map.json exists"
    - "Init command for plan-phase writes function-map-stats.json when function-map.json exists"
    - "SDK builds without TypeScript errors after adding functionMapStats to ContextFiles"
  artifacts:
    - path: "sdk/src/types.ts"
      provides: "functionMapStats optional field on ContextFiles interface"
      contains: "functionMapStats"
    - path: "sdk/src/context-engine.ts"
      provides: "functionMapStats in Execute and Plan manifests"
      contains: "functionMapStats"
    - path: "get-shit-done/bin/lib/init.cjs"
      provides: "Stats file write during init for execute-phase and plan-phase"
      contains: "function-map-stats"
  key_links:
    - from: "sdk/src/context-engine.ts"
      to: "sdk/src/types.ts"
      via: "FileSpec.key references ContextFiles field"
      pattern: "functionMapStats.*function-map-stats"
    - from: "get-shit-done/bin/lib/init.cjs"
      to: "get-shit-done/bin/lib/fmap.cjs"
      via: "calls cmdFmapStats to generate stats file"
      pattern: "cmdFmapStats\\|fmap.*stats"
---

<objective>
Inject Function Map stats into the Context Engine so planners and executors see function coverage at a glance.

Purpose: Per D-06/D-07, inject a lightweight stats summary (not full JSON) into agent context. File-based approach (Approach A from research) matches existing Context Engine pattern — all manifest entries are files.

Output: ContextFiles gains functionMapStats field. PHASE_FILE_MANIFEST for Execute and Plan includes function-map-stats.json. Init commands write the stats file when function-map.json exists.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- From sdk/src/types.ts: ContextFiles interface at line 734 -->
export interface ContextFiles {
  state?: string;
  roadmap?: string;
  context?: string;
  research?: string;
  requirements?: string;
  config?: string;
  plan?: string;
  summary?: string;
}

<!-- From sdk/src/context-engine.ts: FileSpec and PHASE_FILE_MANIFEST -->
interface FileSpec {
  key: keyof ContextFiles;
  filename: string;
  required: boolean;
}

const PHASE_FILE_MANIFEST: Record<PhaseType, FileSpec[]> = {
  [PhaseType.Execute]: [
    { key: 'state', filename: 'STATE.md', required: true },
    { key: 'config', filename: 'config.json', required: false },
  ],
  [PhaseType.Plan]: [
    { key: 'state', filename: 'STATE.md', required: true },
    { key: 'roadmap', filename: 'ROADMAP.md', required: true },
    { key: 'context', filename: 'CONTEXT.md', required: true },
    { key: 'research', filename: 'RESEARCH.md', required: false },
    { key: 'requirements', filename: 'REQUIREMENTS.md', required: false },
  ],
  // ... other phase types
};

<!-- From get-shit-done/bin/lib/fmap.cjs: cmdFmapStats signature -->
function cmdFmapStats(cwd, raw) { ... }
// Returns: { total, by_kind: {function: N, class: N, ...}, path }

<!-- From get-shit-done/bin/lib/init.cjs: cmdInitExecutePhase and cmdInitPlanPhase -->
function cmdInitExecutePhase(cwd, phase, raw) { ... }
function cmdInitPlanPhase(cwd, phase, raw) { ... }
// Both call loadConfig(cwd), build result object, call output(result, raw)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add functionMapStats to ContextFiles and PHASE_FILE_MANIFEST</name>
  <files>sdk/src/types.ts, sdk/src/context-engine.ts, sdk/src/context-engine.test.ts</files>
  <read_first>
    - sdk/src/types.ts (lines 730-745 — ContextFiles interface)
    - sdk/src/context-engine.ts (full file — understand FileSpec, PHASE_FILE_MANIFEST, resolveContextFiles method)
    - sdk/src/context-engine.test.ts (full file — understand existing test patterns for context resolution)
  </read_first>
  <behavior>
    - Test: resolveContextFiles for PhaseType.Execute returns functionMapStats content when .planning/function-map-stats.json exists
    - Test: resolveContextFiles for PhaseType.Execute returns undefined functionMapStats when .planning/function-map-stats.json does not exist
    - Test: resolveContextFiles for PhaseType.Plan returns functionMapStats content when file exists
    - Test: SDK compiles without errors (npm run build in sdk/)
  </behavior>
  <action>
    **Per D-06:**

    1. In `sdk/src/types.ts`, add `functionMapStats?: string;` to the ContextFiles interface (after `summary?: string;`).

    2. In `sdk/src/context-engine.ts`, add to the Execute manifest array (after the config entry):
       ```typescript
       { key: 'functionMapStats', filename: 'function-map-stats.json', required: false },
       ```

    3. In `sdk/src/context-engine.ts`, add to the Plan manifest array (after the requirements entry):
       ```typescript
       { key: 'functionMapStats', filename: 'function-map-stats.json', required: false },
       ```

    4. Add tests in `sdk/src/context-engine.test.ts` that:
       - Create a temporary .planning/ directory with function-map-stats.json containing `{"total":42,"by_kind":{"function":30,"class":12}}`
       - Call resolveContextFiles with PhaseType.Execute and verify functionMapStats is the file contents string
       - Call resolveContextFiles without the file and verify functionMapStats is undefined

    5. Run `cd sdk && npx tsc --noEmit` to verify TypeScript compiles.
  </action>
  <verify>
    <automated>cd /Volumes/SSD/Desenvolvimento/get-shit-done/sdk && npx vitest run src/context-engine.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - sdk/src/types.ts contains `functionMapStats?: string`
    - sdk/src/context-engine.ts Execute manifest contains `functionMapStats`
    - sdk/src/context-engine.ts Plan manifest contains `functionMapStats`
    - sdk/src/context-engine.ts contains `function-map-stats.json`
    - sdk/src/context-engine.test.ts contains test for `functionMapStats`
    - `cd sdk && npx tsc --noEmit` exits 0
    - `cd sdk && npx vitest run src/context-engine.test.ts` exits 0
  </acceptance_criteria>
  <done>ContextFiles interface has functionMapStats field. Execute and Plan manifests include function-map-stats.json as optional file. Tests verify resolution and absence. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Write function-map-stats.json during init commands</name>
  <files>get-shit-done/bin/lib/init.cjs, get-shit-done/bin/lib/fmap.cjs</files>
  <read_first>
    - get-shit-done/bin/lib/init.cjs (full file — understand cmdInitExecutePhase and cmdInitPlanPhase structure, how they build result objects)
    - get-shit-done/bin/lib/fmap.cjs (lines 108-130 — cmdFmapStats implementation, readMap helper)
    - get-shit-done/bin/lib/core.cjs (line 7 — planningRoot export for .planning/ path resolution)
  </read_first>
  <action>
    **Per D-06 (stats file generation during init):**

    In `get-shit-done/bin/lib/init.cjs`:

    1. Add require at top:
       ```javascript
       const { cmdFmapStats } = require('./fmap.cjs');
       ```

    2. In `cmdInitExecutePhase()`, after building the result object but before `output(withProjectRoot(cwd, result), raw)`, add:
       ```javascript
       // Write function-map-stats.json for Context Engine injection (INT-02)
       try {
         const fmapPath = path.join(cwd, '.planning', 'function-map.json');
         if (fs.existsSync(fmapPath)) {
           const stats = cmdFmapStats(cwd, true); // raw=true returns object, not prints
           const statsPath = path.join(cwd, '.planning', 'function-map-stats.json');
           fs.writeFileSync(statsPath, JSON.stringify(stats, null, 2), 'utf-8');
         }
       } catch {
         // Non-fatal: stats file is optional context enrichment
       }
       ```

    3. Add the same block in `cmdInitPlanPhase()`, same position (after result build, before output).

    **Important:** Verify that `cmdFmapStats(cwd, true)` returns a raw JS object (not prints to stdout). Check the `raw` parameter handling in fmap.cjs — if `cmdFmapStats` calls `output(result, raw)` where raw=true returns the object, this works. If not, read the map directly:
       ```javascript
       const mapData = JSON.parse(fs.readFileSync(fmapPath, 'utf-8'));
       const entries = Object.values(mapData);
       const byKind = {};
       for (const e of entries) { byKind[e.kind || 'unknown'] = (byKind[e.kind || 'unknown'] || 0) + 1; }
       const stats = { total: entries.length, by_kind: byKind, path: fmapPath };
       ```

    4. Ensure `fs` and `path` are already required in init.cjs (they should be — verify).
  </action>
  <verify>
    <automated>cd /Volumes/SSD/Desenvolvimento/get-shit-done && node -e "const fs=require('fs'); const p=require('path'); const dir=p.join(process.cwd(),'.planning'); fs.writeFileSync(p.join(dir,'function-map.json'), JSON.stringify({'test::fn':{kind:'function',signature:'fn()',callers:[],calls:[]}})); const {execSync}=require('child_process'); execSync('node get-shit-done/bin/gsd-tools.cjs init execute-phase 03', {cwd:process.cwd()}); const exists=fs.existsSync(p.join(dir,'function-map-stats.json')); console.log('stats file exists:', exists); if(!exists) process.exit(1); const stats=JSON.parse(fs.readFileSync(p.join(dir,'function-map-stats.json'),'utf-8')); console.log('stats:', JSON.stringify(stats)); if(!stats.total) process.exit(1); fs.unlinkSync(p.join(dir,'function-map-stats.json')); fs.unlinkSync(p.join(dir,'function-map.json')); console.log('PASS')"</automated>
  </verify>
  <acceptance_criteria>
    - init.cjs contains `function-map-stats.json` string
    - init.cjs contains `cmdFmapStats` or equivalent stats generation logic
    - init.cjs cmdInitExecutePhase writes stats file when function-map.json exists
    - init.cjs cmdInitPlanPhase writes stats file when function-map.json exists
    - Running `gsd-tools init execute-phase 03` with a function-map.json present produces .planning/function-map-stats.json
    - The stats file contains `total` and `by_kind` keys
  </acceptance_criteria>
  <done>Init commands write function-map-stats.json to .planning/ when function-map.json exists. Context Engine can now resolve this file for planners and executors. File is transient (regenerated each run).</done>
</task>

</tasks>

<verification>
- `cd sdk && npx vitest run src/context-engine.test.ts` — context engine tests pass
- `cd sdk && npx tsc --noEmit` — TypeScript compiles
- Init execute-phase produces function-map-stats.json when function-map.json exists
- Init plan-phase produces function-map-stats.json when function-map.json exists
</verification>

<success_criteria>
- Planners and executors receive function map statistics in their context automatically
- No TypeScript compilation errors in SDK
- Stats file is lightweight (under 500 bytes) and regenerated each run
- Missing function-map.json gracefully results in no stats file (no errors)
</success_criteria>

<output>
After completion, create `.planning/phases/03-model-routing-integration/03-02-SUMMARY.md`
</output>
