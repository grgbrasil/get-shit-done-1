---
phase: 03-model-routing-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/bin/lib/config.cjs
  - get-shit-done/bin/lib/core.cjs
  - tests/config.test.cjs
  - tests/core.test.cjs
autonomous: true
requirements: [MODEL-01, MODEL-02, MODEL-03, MODEL-04]
must_haves:
  truths:
    - "config.json contains model_overrides key with empty object default for new projects"
    - "config.json contains impact_analysis section with enabled:false, auto_resolve_threshold:10, escalation_threshold:50 defaults"
    - "loadConfig returns model_overrides as {} (not null) when key is absent from config.json"
    - "loadConfig returns impact_analysis with full defaults when key is absent from config.json"
    - "gsd-tools config-set model_overrides.gsd-cataloger haiku succeeds (dynamic key pattern)"
    - "gsd-tools config-set impact_analysis.enabled true succeeds (registered in VALID_CONFIG_KEYS)"
  artifacts:
    - path: "get-shit-done/bin/lib/config.cjs"
      provides: "model_overrides and impact_analysis in hardcoded defaults + three-level merge + VALID_CONFIG_KEYS"
      contains: "model_overrides"
    - path: "get-shit-done/bin/lib/core.cjs"
      provides: "loadConfig returns model_overrides as {} and impact_analysis with defaults"
      contains: "impact_analysis"
    - path: "tests/config.test.cjs"
      provides: "Tests for buildNewProjectConfig model_overrides and impact_analysis defaults"
      contains: "impact_analysis"
    - path: "tests/core.test.cjs"
      provides: "Updated test: model_overrides defaults to {} not null"
      contains: "model_overrides"
  key_links:
    - from: "get-shit-done/bin/lib/config.cjs"
      to: "config.json"
      via: "buildNewProjectConfig hardcoded defaults"
      pattern: "model_overrides.*\\{\\}"
    - from: "get-shit-done/bin/lib/core.cjs"
      to: "config.json"
      via: "loadConfig defaults"
      pattern: "impact_analysis.*enabled.*false"
---

<objective>
Extend GSD config system with model_overrides and impact_analysis defaults.

Purpose: Per-agent model configuration (MODEL-01/02/04) and impact analysis toggle (MODEL-03 is docs-only, handled in Plan 03) require config.json to have proper defaults in both config paths: buildNewProjectConfig (new projects) and loadConfig (existing projects).

Output: Both config paths produce model_overrides: {} and impact_analysis: { enabled: false, auto_resolve_threshold: 10, escalation_threshold: 50 } by default. VALID_CONFIG_KEYS accepts dynamic model_overrides.<agent> keys and impact_analysis.* keys.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- From config.cjs: hardcoded defaults object at line ~103 -->
const hardcoded = {
  model_profile: 'balanced',
  commit_docs: true,
  parallelization: true,
  // ... other keys ...
  agent_skills: {},
};
// Three-level merge at line ~139:
return {
  ...hardcoded, ...userDefaults, ...choices,
  git: { ...hardcoded.git, ...(userDefaults.git || {}), ...(choices.git || {}) },
  workflow: { ...hardcoded.workflow, ...(userDefaults.workflow || {}), ...(choices.workflow || {}) },
  hooks: { ...hardcoded.hooks, ...(userDefaults.hooks || {}), ...(choices.hooks || {}) },
  agent_skills: { ...hardcoded.agent_skills, ...(userDefaults.agent_skills || {}), ...(choices.agent_skills || {}) },
};

<!-- From config.cjs: VALID_CONFIG_KEYS at line ~14 -->
const VALID_CONFIG_KEYS = new Set([...]);
function isValidConfigKey(keyPath) {
  if (VALID_CONFIG_KEYS.has(keyPath)) return true;
  if (/^agent_skills\.[a-zA-Z0-9_-]+$/.test(keyPath)) return true;
  return false;
}

<!-- From core.cjs: loadConfig return at line ~282 -->
return {
  // ... other keys ...
  model_overrides: parsed.model_overrides || null,
  agent_skills: parsed.agent_skills || {},
};
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add model_overrides and impact_analysis to config defaults + VALID_CONFIG_KEYS</name>
  <files>get-shit-done/bin/lib/config.cjs, get-shit-done/bin/lib/core.cjs, tests/config.test.cjs, tests/core.test.cjs</files>
  <read_first>
    - get-shit-done/bin/lib/config.cjs (full file — understand hardcoded defaults object at ~line 103, three-level merge at ~line 139, VALID_CONFIG_KEYS at ~line 14, isValidConfigKey at ~line 35)
    - get-shit-done/bin/lib/core.cjs (lines 280-315 — loadConfig return statement with model_overrides fallback)
    - tests/config.test.cjs (full file — understand existing test patterns)
    - tests/core.test.cjs (lines 85-100 — existing model_overrides tests that assert null; these must change to {})
  </read_first>
  <behavior>
    - Test: buildNewProjectConfig({}, {}) returns object with model_overrides key equal to {}
    - Test: buildNewProjectConfig({}, {}) returns object with impact_analysis.enabled === false
    - Test: buildNewProjectConfig({}, {}) returns object with impact_analysis.auto_resolve_threshold === 10
    - Test: buildNewProjectConfig({}, {}) returns object with impact_analysis.escalation_threshold === 50
    - Test: buildNewProjectConfig({}, { impact_analysis: { enabled: true } }) returns impact_analysis.enabled === true (three-level merge works)
    - Test: buildNewProjectConfig({ model_overrides: { 'gsd-cataloger': 'haiku' } }, {}) preserves user default override
    - Test: isValidConfigKey('impact_analysis.enabled') returns true
    - Test: isValidConfigKey('impact_analysis.auto_resolve_threshold') returns true
    - Test: isValidConfigKey('impact_analysis.escalation_threshold') returns true
    - Test: isValidConfigKey('model_overrides.gsd-cataloger') returns true
    - Test: isValidConfigKey('model_overrides.gsd-executor') returns true
    - Test: isValidConfigKey('model_overrides') returns false (bare key without agent suffix)
    - Test (core.test.cjs): loadConfig returns model_overrides as {} (not null) when absent — UPDATE existing test at line 95-98
    - Test (core.test.cjs): loadConfig returns impact_analysis with defaults when absent from config.json
    - Test (core.test.cjs): loadConfig returns impact_analysis from config.json when present
  </behavior>
  <action>
    **Per D-01, D-02, D-03, D-14, D-16:**

    **config.cjs changes:**

    1. Add to VALID_CONFIG_KEYS Set (after 'hooks.context_warnings'):
       ```
       'impact_analysis.enabled',
       'impact_analysis.auto_resolve_threshold',
       'impact_analysis.escalation_threshold',
       ```

    2. Add dynamic pattern to isValidConfigKey() (after the agent_skills regex):
       ```javascript
       if (/^model_overrides\.[a-zA-Z0-9_-]+$/.test(keyPath)) return true;
       ```

    3. Add to hardcoded defaults object (after `agent_skills: {}`):
       ```javascript
       model_overrides: {},
       impact_analysis: {
         enabled: false,
         auto_resolve_threshold: 10,
         escalation_threshold: 50,
       },
       ```

    4. Add to three-level merge return (after agent_skills merge block):
       ```javascript
       model_overrides: {
         ...hardcoded.model_overrides,
         ...(userDefaults.model_overrides || {}),
         ...(choices.model_overrides || {}),
       },
       impact_analysis: {
         ...hardcoded.impact_analysis,
         ...(userDefaults.impact_analysis || {}),
         ...(choices.impact_analysis || {}),
       },
       ```

    **core.cjs changes:**

    5. In loadConfig return statement (~line 311), change:
       `model_overrides: parsed.model_overrides || null,`
       to:
       `model_overrides: parsed.model_overrides || {},`

    6. Add after the model_overrides line:
       ```javascript
       impact_analysis: parsed.impact_analysis || {
         enabled: false,
         auto_resolve_threshold: 10,
         escalation_threshold: 50,
       },
       ```

    **Test changes:**

    7. In tests/core.test.cjs, update test at ~line 95-98: change assertion from `assert.strictEqual(config.model_overrides, null)` to `assert.deepStrictEqual(config.model_overrides, {})` and rename test to 'returns model_overrides as empty object when not in config'.

    8. Add new tests in tests/core.test.cjs for impact_analysis defaults.

    9. Add new tests in tests/config.test.cjs for buildNewProjectConfig and isValidConfigKey.
  </action>
  <verify>
    <automated>cd /Volumes/SSD/Desenvolvimento/get-shit-done && node --test tests/config.test.cjs && node --test tests/core.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - config.cjs contains `model_overrides: {},` inside the hardcoded defaults object
    - config.cjs contains `impact_analysis:` with `enabled: false` inside hardcoded defaults
    - config.cjs contains `auto_resolve_threshold: 10` inside hardcoded defaults
    - config.cjs contains `escalation_threshold: 50` inside hardcoded defaults
    - config.cjs three-level merge contains `...hardcoded.impact_analysis`
    - config.cjs three-level merge contains `...hardcoded.model_overrides`
    - config.cjs VALID_CONFIG_KEYS contains 'impact_analysis.enabled'
    - config.cjs isValidConfigKey contains regex pattern `model_overrides\\.`
    - core.cjs contains `parsed.model_overrides || {}` (not `|| null`)
    - core.cjs contains `parsed.impact_analysis ||`
    - tests/config.test.cjs contains 'impact_analysis' test assertions
    - tests/config.test.cjs contains 'model_overrides' test assertions
    - tests/core.test.cjs contains `deepStrictEqual(config.model_overrides, {})` (updated from null)
    - `node --test tests/config.test.cjs` exits 0
    - `node --test tests/core.test.cjs` exits 0
  </acceptance_criteria>
  <done>
    buildNewProjectConfig produces model_overrides:{} and impact_analysis with all three sub-keys by default. loadConfig returns {} instead of null for missing model_overrides, and returns full impact_analysis defaults when absent. VALID_CONFIG_KEYS accepts impact_analysis.* and model_overrides.<agent> patterns. All tests pass.
  </done>
</task>

</tasks>

<verification>
- `node --test tests/config.test.cjs` — all config tests pass
- `node --test tests/core.test.cjs` — all core tests pass including updated model_overrides default
- `grep -n 'model_overrides' get-shit-done/bin/lib/config.cjs` — shows in hardcoded defaults and merge
- `grep -n 'impact_analysis' get-shit-done/bin/lib/config.cjs` — shows in hardcoded defaults, merge, and VALID_CONFIG_KEYS
- `grep -n 'impact_analysis' get-shit-done/bin/lib/core.cjs` — shows in loadConfig return
</verification>

<success_criteria>
- New projects get model_overrides:{} and impact_analysis section in config.json
- Existing projects without these keys get sensible defaults from loadConfig
- config-set commands work for impact_analysis.enabled and model_overrides.<agent>
- All existing tests still pass (no regression)
</success_criteria>

<output>
After completion, create `.planning/phases/03-model-routing-integration/03-01-SUMMARY.md`
</output>
