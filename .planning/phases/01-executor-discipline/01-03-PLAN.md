---
phase: 01-executor-discipline
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/bin/lib/core.cjs
  - get-shit-done/workflows/execute-phase.md
  - commands/gsd/execute-phase.md
  - get-shit-done/templates/summary.md
autonomous: true
requirements:
  - SCOPE-03
  - SCOPE-05

must_haves:
  truths:
    - "maxTurns config exists with three complexity tiers: simple=30, medium=100, complex=200"
    - "Execute-phase workflow propagates maxTurns to executor subagents based on plan complexity"
    - "Phase transitions use structured 9-section handoff summary format"
    - "Execute-phase command includes context budget awareness instructions"
  artifacts:
    - path: "get-shit-done/bin/lib/core.cjs"
      provides: "MAX_TURNS constant with complexity tiers"
      contains: "MAX_TURNS"
    - path: "get-shit-done/workflows/execute-phase.md"
      provides: "maxTurns propagation and structured handoff template"
      contains: "MAX_TURNS"
    - path: "commands/gsd/execute-phase.md"
      provides: "Context budget awareness note"
      contains: "context budget"
    - path: "get-shit-done/templates/summary.md"
      provides: "Structured handoff section in summary template"
      contains: "Phase Handoff"
  key_links:
    - from: "get-shit-done/bin/lib/core.cjs"
      to: "get-shit-done/workflows/execute-phase.md"
      via: "MAX_TURNS constant referenced in workflow"
      pattern: "MAX_TURNS"
    - from: "get-shit-done/workflows/execute-phase.md"
      to: "agents/gsd-executor.md"
      via: "maxTurns passed in subagent spawn prompt"
      pattern: "maxTurns"
---

<objective>
Add maxTurns configuration per plan complexity tier and structured phase handoff summaries to the GSD workflow.

Purpose: Prevent runaway executor loops (maxTurns) and preserve critical context across phase transitions (structured handoff).
Output: Updated core config, execute-phase workflow, execute-phase command, and summary template.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/PHASE-SCOPING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add MAX_TURNS config to core.cjs and propagate in execute-phase workflow</name>
  <files>get-shit-done/bin/lib/core.cjs, get-shit-done/workflows/execute-phase.md, commands/gsd/execute-phase.md</files>
  <read_first>
    - get-shit-done/bin/lib/core.cjs (full file — understand loadConfig defaults structure and module.exports)
    - get-shit-done/workflows/execute-phase.md (full file — find where subagents are spawned, understand wave execution flow)
    - commands/gsd/execute-phase.md (full file — understand command structure for context budget note)
    - .planning/research/PHASE-SCOPING.md (section 1.4 — maxTurns limits from Claude Code)
  </read_first>
  <action>
**1. Add MAX_TURNS constant to `get-shit-done/bin/lib/core.cjs`:**

Add a new constant block AFTER the existing `require` statements (after line 8) and BEFORE the path helpers section:

```javascript
// ─── Turn Limits ────────────────────────────────────────────────────────────

/**
 * Maximum agent turns per plan complexity tier.
 * Prevents runaway execution loops. Based on Claude Code fork agent limits.
 * Simple: config/styling/CRUD. Medium: business logic/API. Complex: auth/payments/migrations.
 */
const MAX_TURNS = {
  simple: 30,
  medium: 100,
  complex: 200,
};
```

Add `MAX_TURNS` to `module.exports` at the bottom of the file (in the existing exports object).

**2. Propagate maxTurns in `get-shit-done/workflows/execute-phase.md`:**

Find the section where executor subagents are spawned (step 3, around the Task spawning). In the executor prompt/context that gets passed to the subagent, add a note about turn limits. Find the `<files_to_read>` block or the prompt construction for the executor, and add after it:

```markdown
**Turn limit:** This plan has complexity tier `{complexity from plan frontmatter, default: medium}`. Maximum turns: {MAX_TURNS[complexity]}. If you approach this limit, prioritize completing the current task and committing over starting new tasks.
```

Also add to the plan frontmatter parsing section (where `autonomous`, `wave`, etc. are read): extract `complexity` field (default: `"medium"` if not present).

**3. Add context budget awareness to `commands/gsd/execute-phase.md`:**

In the `<objective>` section, after the existing "Context budget: ~15% orchestrator, 100% fresh per subagent." line, add:

```markdown
Turn limits: Executors have maxTurns per plan complexity (simple: 30, medium: 100, complex: 200). When approaching the turn limit, the executor should prioritize completing and committing the current task over starting new ones. Complexity is read from plan frontmatter `complexity` field (default: medium).
```
  </action>
  <verify>
    <automated>grep -c "MAX_TURNS" get-shit-done/bin/lib/core.cjs && grep -c "simple.*30" get-shit-done/bin/lib/core.cjs && grep -c "Turn limit" get-shit-done/workflows/execute-phase.md && grep -c "Turn limits" commands/gsd/execute-phase.md</automated>
  </verify>
  <acceptance_criteria>
    - get-shit-done/bin/lib/core.cjs contains `const MAX_TURNS = {`
    - get-shit-done/bin/lib/core.cjs contains `simple: 30,`
    - get-shit-done/bin/lib/core.cjs contains `medium: 100,`
    - get-shit-done/bin/lib/core.cjs contains `complex: 200,`
    - get-shit-done/bin/lib/core.cjs exports MAX_TURNS in module.exports
    - get-shit-done/workflows/execute-phase.md contains "Turn limit" with complexity tier reference
    - commands/gsd/execute-phase.md contains "Turn limits" with the three tier values
  </acceptance_criteria>
  <done>maxTurns config exists with three tiers and is propagated to executor subagents via the execute-phase workflow</done>
</task>

<task type="auto">
  <name>Task 2: Add structured 9-section handoff to summary template and wave reports</name>
  <files>get-shit-done/templates/summary.md, get-shit-done/workflows/execute-phase.md</files>
  <read_first>
    - get-shit-done/templates/summary.md (current template structure)
    - get-shit-done/workflows/execute-phase.md (wave completion report format, around line 390-405)
    - .planning/research/PHASE-SCOPING.md (section 2.2 — compaction prompt 9 sections)
  </read_first>
  <action>
**1. Add structured handoff section to `get-shit-done/templates/summary.md`:**

Add a new section to the template AFTER the "Next Phase Readiness" section and BEFORE the closing `---` line. This section is the structured phase handoff summary based on Claude Code's 9-section compact template:

```markdown
## Phase Handoff Summary

Structured context for the next phase (or for context recovery after compaction):

1. **Primary Request:** [What was this phase asked to accomplish — the goal from ROADMAP.md]
2. **Key Technical Concepts:** [Technologies, frameworks, patterns used or established]
3. **Files and Code Sections:** [Critical file paths and their roles — not all files, just the ones the next phase needs]
4. **Errors and Fixes:** [Problems encountered and how they were resolved — saves the next phase from repeating mistakes]
5. **Problem Solving:** [Design decisions, tradeoffs considered, alternatives rejected and why]
6. **User Decisions:** [Any user decisions made during execution that constrain future work]
7. **Pending Tasks:** [Anything deferred, known stubs, incomplete items carried forward]
8. **Current State:** [What the system looks like NOW — working features, known limitations]
9. **Next Step:** [Concrete next action for the following phase — not vague "continue development"]
```

Also add to the `<frontmatter_guidance>` section a note:

```markdown
**Phase Handoff Summary:** The 9-section handoff at the bottom preserves critical context for phase transitions. Each section should be 1-3 sentences. Empty sections should say "None" rather than being omitted. This format is based on Claude Code's compaction prompt structure.
```

**2. Update wave completion report in `get-shit-done/workflows/execute-phase.md`:**

Find the wave completion report template (around line 392-404, the `## Wave {N} Complete` block). Enhance it to include a mini-handoff when the wave is the LAST wave in the phase. After the existing wave report format, add:

```markdown
   {If this is the LAST wave in the phase, also include:}

   **Handoff to verification:**
   - **What was built:** [1-2 sentence summary from SUMMARYs]
   - **Known issues:** [From deviation sections, or "None"]
   - **Files to verify:** [Top 5 files from key-files across all plans]
```
  </action>
  <verify>
    <automated>grep -c "Phase Handoff Summary" get-shit-done/templates/summary.md && grep -c "Primary Request" get-shit-done/templates/summary.md && grep -c "Handoff to verification" get-shit-done/workflows/execute-phase.md</automated>
  </verify>
  <acceptance_criteria>
    - get-shit-done/templates/summary.md contains "## Phase Handoff Summary"
    - get-shit-done/templates/summary.md contains all 9 numbered sections (Primary Request through Next Step)
    - get-shit-done/templates/summary.md frontmatter_guidance mentions "Phase Handoff Summary"
    - get-shit-done/workflows/execute-phase.md contains "Handoff to verification" in the wave completion section
    - get-shit-done/workflows/execute-phase.md contains "Known issues" in the handoff block
  </acceptance_criteria>
  <done>Summary template has 9-section structured handoff format, wave reports include handoff context for last-wave completion</done>
</task>

</tasks>

<verification>
All SCOPE-03 and SCOPE-05 artifacts are in place:
- `grep "MAX_TURNS" get-shit-done/bin/lib/core.cjs` returns the constant
- `grep "Phase Handoff Summary" get-shit-done/templates/summary.md` returns the section
- `grep "Turn limit" get-shit-done/workflows/execute-phase.md` returns the propagation
- `grep "Turn limits" commands/gsd/execute-phase.md` returns the awareness note
- `grep "Handoff to verification" get-shit-done/workflows/execute-phase.md` returns the handoff block
</verification>

<success_criteria>
- maxTurns configured with three tiers (simple/medium/complex) in core.cjs
- Execute-phase workflow propagates turn limits to executor subagents
- Execute-phase command documents turn limit behavior
- Summary template includes 9-section structured handoff format
- Wave completion reports include handoff context for phase transitions
</success_criteria>

<output>
After completion, create `.planning/phases/01-executor-discipline/01-03-SUMMARY.md`
</output>
