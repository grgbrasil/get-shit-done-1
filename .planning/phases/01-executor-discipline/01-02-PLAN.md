---
phase: 01-executor-discipline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/gsd-planner.md
autonomous: true
requirements:
  - SCOPE-04

must_haves:
  truths:
    - "Planner synthesizes research findings itself before creating plans — never passes raw research to executor"
    - "A synthesis artifact exists between research and plan creation"
  artifacts:
    - path: "agents/gsd-planner.md"
      provides: "Synthesis step between research loading and plan creation"
      contains: "synthesis"
  key_links:
    - from: "agents/gsd-planner.md"
      to: "gather_phase_context step"
      via: "synthesis step inserted after context gathering"
      pattern: "synthesize"
---

<objective>
Add a synthesis step to the GSD planner agent between research/context gathering and plan creation — implementing the Claude Code coordinator pattern of "never delegate understanding".

Purpose: The planner must synthesize research findings itself rather than passing raw research to executors. This produces better plans because the planner understands WHY, not just WHAT.
Output: Updated `agents/gsd-planner.md` with a new synthesis step in the execution flow.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/PHASE-SCOPING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add synthesis step to planner execution flow</name>
  <files>agents/gsd-planner.md</files>
  <read_first>
    - agents/gsd-planner.md (full file — understand execution_flow structure, all existing steps)
    - .planning/research/PHASE-SCOPING.md (section 3.1 — coordinator "never delegate understanding" pattern)
  </read_first>
  <action>
Add a new step to the `<execution_flow>` section of `agents/gsd-planner.md`. Insert it AFTER the `gather_phase_context` step and BEFORE the `break_into_tasks` step.

The new step:

```xml
<step name="synthesize_understanding">
**"Never delegate understanding."** Before creating any tasks, synthesize what you learned from research and context into a coherent mental model. Do NOT pass raw research findings to executors — YOU must understand the problem space.

After reading CONTEXT.md, RESEARCH.md, and DISCOVERY.md, produce a synthesis block in your reasoning:

1. **Problem statement** — What is this phase solving? (1-2 sentences, in your own words, not copied from research)
2. **Key constraints** — What limits the solution space? (from CONTEXT.md decisions, CLAUDE.md rules, codebase patterns)
3. **Technical approach** — How should this be built? (synthesized from research findings, not raw quotes)
4. **Risk areas** — Where is this most likely to break? (your assessment, informed by research)
5. **What executors need to know** — Critical context that must be in task actions (not in @-references executors might not read)

**Anti-pattern:** Creating a plan that says "See RESEARCH.md for details" or "Follow the pattern described in the research". The executor starts with zero context — everything they need must be IN the plan actions.

**Anti-pattern:** Copying research findings verbatim into plan actions. Synthesize: extract the actionable parts, combine with codebase knowledge, produce specific instructions.

This synthesis is internal (not written to a file) but directly shapes every task action you write. If you cannot articulate the technical approach in your own words, you do not understand it well enough to plan it.
</step>
```

Place this step between `gather_phase_context` and `break_into_tasks` in the execution_flow. The step must be a sibling of the other steps (same indentation level, inside `<execution_flow>`).
  </action>
  <verify>
    <automated>grep -c "synthesize_understanding" agents/gsd-planner.md && grep -c "Never delegate understanding" agents/gsd-planner.md && grep -c "Anti-pattern" agents/gsd-planner.md</automated>
  </verify>
  <acceptance_criteria>
    - agents/gsd-planner.md contains `<step name="synthesize_understanding">`
    - agents/gsd-planner.md contains "Never delegate understanding"
    - agents/gsd-planner.md contains "Problem statement"
    - agents/gsd-planner.md contains "What executors need to know"
    - agents/gsd-planner.md contains both "Anti-pattern" warnings (raw research refs and verbatim copying)
    - The synthesize_understanding step appears AFTER gather_phase_context and BEFORE break_into_tasks in the file
  </acceptance_criteria>
  <done>Planner has a mandatory synthesis step that forces it to understand research before planning — "never delegate understanding" pattern implemented</done>
</task>

</tasks>

<verification>
- `grep -n "synthesize_understanding" agents/gsd-planner.md` returns line number between gather_phase_context and break_into_tasks
- `grep "Never delegate understanding" agents/gsd-planner.md` returns match
- The planner execution flow now has the sequence: gather_phase_context -> synthesize_understanding -> break_into_tasks
</verification>

<success_criteria>
- Planner agent has a synthesis step between research gathering and task breakdown
- The step explicitly forbids delegating raw research to executors
- The step requires the planner to articulate understanding in its own words before planning
</success_criteria>

<output>
After completion, create `.planning/phases/01-executor-discipline/01-02-SUMMARY.md`
</output>
