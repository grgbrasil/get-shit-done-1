---
phase: 01-executor-discipline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/gsd-executor.md
autonomous: true
requirements:
  - SCOPE-01
  - SCOPE-02
  - SCOPE-06

must_haves:
  truths:
    - "Executor declares its scope at the start of every plan execution before touching any files"
    - "Executor cannot report plan complete without a commit hash for every task"
    - "Executor persists critical findings to STATE.md or plan notes before context decay"
  artifacts:
    - path: "agents/gsd-executor.md"
      provides: "Scope echo, commit-before-report enforcement, micro-compact awareness instructions"
      contains: "Scope:"
  key_links:
    - from: "agents/gsd-executor.md"
      to: "executor behavior at runtime"
      via: "prompt instructions"
      pattern: "Scope:"
    - from: "agents/gsd-executor.md"
      to: "task_commit_protocol"
      via: "commit-before-report gate"
      pattern: "commit hash"
---

<objective>
Add three discipline patterns from Claude Code to the GSD executor agent: scope echo (declare scope before executing), commit-before-report (no "done" without commit hash), and micro-compact awareness (persist findings before context decay).

Purpose: Reduce scope drift, ensure verifiable commits on every task, prevent context decay from erasing critical findings.
Output: Updated `agents/gsd-executor.md` with three new behavioral sections.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/PHASE-SCOPING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add scope echo and micro-compact awareness to executor agent</name>
  <files>agents/gsd-executor.md</files>
  <read_first>
    - agents/gsd-executor.md (current executor agent — understand full structure before modifying)
    - .planning/research/PHASE-SCOPING.md (source patterns from Claude Code fork children)
  </read_first>
  <action>
Add two new sections to `agents/gsd-executor.md`:

**1. Scope Echo (insert as new step in execution_flow, BEFORE `execute_tasks`, AFTER `determine_execution_pattern`):**

Add a new step:

```xml
<step name="declare_scope">
Before executing any task, state your scope explicitly. Your first output MUST begin with:

```
Scope: [echo back the plan objective from frontmatter]
Plan: {phase}-{plan}
Tasks: {count} tasks
Files: {list of files_modified from frontmatter}
```

This catches scope misunderstanding before execution begins. Do NOT skip this step. Do NOT begin tool calls before declaring scope.

If the scope in the plan conflicts with what you observe in the codebase, STOP and report the conflict instead of proceeding with assumptions.
</step>
```

**2. Micro-compact awareness (insert as new section AFTER `analysis_paralysis_guard`, BEFORE `authentication_gates`):**

```xml
<context_persistence>
**Write down critical findings before they decay.**

Tool results (Read, Bash, Grep, Glob outputs) may be cleared from context as execution progresses. Before moving to the next task:

1. **Extract key values** — config paths, function signatures, API responses, error messages
2. **Write to plan notes** — append to `.planning/phases/{phase-dir}/execution-notes.md` if findings are needed by later tasks
3. **Update STATE.md** — use `state add-decision` for architectural discoveries that affect future phases

**Rule:** If you read a file and found critical information (a type signature, a config value, an API contract), write it into your current task's commit message or execution notes IMMEDIATELY. Do not assume you can re-read the file later — context pressure may prevent it.

**Trigger:** After any Bash/Read call that returns data you will need 3+ tasks later, persist it now.
</context_persistence>
```

Both sections must be valid XML within the existing document structure. Maintain the 2-space indentation and markdown-in-XML style used throughout the file.
  </action>
  <verify>
    <automated>grep -c "declare_scope" agents/gsd-executor.md && grep -c "context_persistence" agents/gsd-executor.md && grep -c "Scope:" agents/gsd-executor.md</automated>
  </verify>
  <acceptance_criteria>
    - agents/gsd-executor.md contains `<step name="declare_scope">`
    - agents/gsd-executor.md contains `Scope:` in the declare_scope step
    - agents/gsd-executor.md contains `<context_persistence>`
    - agents/gsd-executor.md contains "Write down critical findings before they decay"
    - agents/gsd-executor.md contains "execution-notes.md"
    - The declare_scope step appears AFTER determine_execution_pattern and BEFORE execute_tasks
    - The context_persistence section appears AFTER analysis_paralysis_guard and BEFORE authentication_gates
  </acceptance_criteria>
  <done>Executor agent has scope echo step and micro-compact awareness section with concrete instructions</done>
</task>

<task type="auto">
  <name>Task 2: Add commit-before-report enforcement to executor agent</name>
  <files>agents/gsd-executor.md</files>
  <read_first>
    - agents/gsd-executor.md (re-read after Task 1 modifications)
    - .planning/research/PHASE-SCOPING.md (section 1.2 — fork child rules for commit-before-report pattern)
  </read_first>
  <action>
Modify two existing sections in `agents/gsd-executor.md` to enforce commit-before-report:

**1. Update `<completion_format>` section — add commit verification gate:**

Replace the existing completion_format content with:

```xml
<completion_format>
**COMMIT-BEFORE-REPORT GATE:** Before generating this completion message, verify EVERY task has a commit hash. Run:

```bash
git log --oneline -${TASK_COUNT} | head -${TASK_COUNT}
```

If any task lacks a commit hash, DO NOT report completion. Instead, commit the uncommitted work first, then report.

```markdown
## PLAN COMPLETE

**Plan:** {phase}-{plan}
**Tasks:** {completed}/{total}
**SUMMARY:** {path to SUMMARY.md}

**Commits:**
- {hash}: {message}
- {hash}: {message}

**Files changed:** {list of all files with their commit hashes}
**Duration:** {time}
```

Include ALL commits (previous + new if continuation agent). Every task MUST have a corresponding commit hash in this list. A completion report without commit hashes for all tasks is INVALID.
</completion_format>
```

**2. Update `<success_criteria>` section — add commit verification criterion:**

Add this line to the success_criteria checklist, after the "Each task committed individually" line:

```
- [ ] Completion report includes commit hash for every task (commit-before-report gate passed)
```

Do NOT remove any existing criteria. Only add the new line.
  </action>
  <verify>
    <automated>grep -c "COMMIT-BEFORE-REPORT GATE" agents/gsd-executor.md && grep -c "commit-before-report gate passed" agents/gsd-executor.md && grep -c "Files changed:" agents/gsd-executor.md</automated>
  </verify>
  <acceptance_criteria>
    - agents/gsd-executor.md contains "COMMIT-BEFORE-REPORT GATE"
    - agents/gsd-executor.md contains "commit-before-report gate passed" in success_criteria
    - agents/gsd-executor.md contains "Files changed:" in completion_format
    - agents/gsd-executor.md contains "A completion report without commit hashes for all tasks is INVALID"
    - The completion_format still contains the original markdown template structure (Plan, Tasks, SUMMARY, Commits, Duration)
  </acceptance_criteria>
  <done>Executor cannot report "done" without commit hash for every task, enforced via gate check and success criteria</done>
</task>

</tasks>

<verification>
All three discipline patterns (SCOPE-01, SCOPE-02, SCOPE-06) are present in gsd-executor.md:
- `grep "declare_scope" agents/gsd-executor.md` returns match
- `grep "COMMIT-BEFORE-REPORT" agents/gsd-executor.md` returns match
- `grep "context_persistence" agents/gsd-executor.md` returns match
- The file remains valid markdown with proper XML structure
</verification>

<success_criteria>
- Executor agent declares scope before any execution (scope echo)
- Executor agent cannot report completion without commit hashes (commit-before-report)
- Executor agent has instructions to persist findings before context decay (micro-compact)
- All three patterns are concrete instructions, not vague guidelines
</success_criteria>

<output>
After completion, create `.planning/phases/01-executor-discipline/01-01-SUMMARY.md`
</output>
