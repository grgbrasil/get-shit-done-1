---
phase: 04-brainstorm-phase-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/bin/lib/core.cjs
  - get-shit-done/bin/lib/init.cjs
  - get-shit-done/workflows/discuss-phase.md
  - get-shit-done/workflows/discuss-phase-assumptions.md
  - get-shit-done/workflows/do.md
  - get-shit-done/workflows/help.md
autonomous: true
requirements: [BRAIN-02, BRAIN-03]

must_haves:
  truths:
    - "getPhaseFileStats() detects -BRAINSTORM.md files and returns hasBrainstorm: true"
    - "gsd-tools.cjs init phase-op N returns has_brainstorm: true when BRAINSTORM.md exists in phase dir"
    - "discuss-phase reads same-phase BRAINSTORM.md and treats decisions as Likely confidence assumptions"
    - "discuss-phase-assumptions reads same-phase BRAINSTORM.md identically"
    - "do.md routes brainstorming intent to /gsd:brainstorm-phase"
    - "help.md lists /gsd:brainstorm-phase in the command reference"
  artifacts:
    - path: "get-shit-done/bin/lib/core.cjs"
      provides: "hasBrainstorm detection in getPhaseFileStats"
      contains: "hasBrainstorm"
    - path: "get-shit-done/bin/lib/init.cjs"
      provides: "has_brainstorm boolean in all init outputs"
      contains: "has_brainstorm"
    - path: "get-shit-done/workflows/discuss-phase.md"
      provides: "BRAINSTORM.md detection in load_prior_context"
      contains: "BRAINSTORM.md"
    - path: "get-shit-done/workflows/discuss-phase-assumptions.md"
      provides: "BRAINSTORM.md detection in load_prior_context"
      contains: "BRAINSTORM.md"
    - path: "get-shit-done/workflows/do.md"
      provides: "Routing for brainstorming intent"
      contains: "brainstorm-phase"
    - path: "get-shit-done/workflows/help.md"
      provides: "Command listing for brainstorm-phase"
      contains: "brainstorm-phase"
  key_links:
    - from: "get-shit-done/bin/lib/core.cjs"
      to: "get-shit-done/bin/lib/init.cjs"
      via: "getPhaseFileStats().hasBrainstorm consumed by init output"
      pattern: "has_brainstorm.*hasBrainstorm"
    - from: "get-shit-done/bin/lib/init.cjs"
      to: "get-shit-done/workflows/brainstorm-phase.md"
      via: "has_brainstorm boolean consumed by workflow check_existing step"
      pattern: "has_brainstorm"
    - from: "get-shit-done/workflows/discuss-phase.md"
      to: "BRAINSTORM.md artifact"
      via: "load_prior_context reads design_decisions and pre_context tags"
      pattern: "design_decisions|pre_context"
---

<objective>
Wire brainstorm-phase into GSD infrastructure: artifact detection in core.cjs/init.cjs, integration patches in both discuss-phase workflows, and routing updates in do.md/help.md.

Purpose: Without these patches, brainstorm-phase exists but is invisible to the rest of GSD -- init won't detect BRAINSTORM.md, discuss-phase won't read it, and users can't discover the command.

Output: Six existing files patched to recognize and route brainstorm-phase artifacts and commands.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-brainstorm-phase-integration/04-CONTEXT.md
@.planning/phases/04-brainstorm-phase-integration/04-RESEARCH.md

<interfaces>
<!-- core.cjs getPhaseFileStats() at line ~1183 -->
From get-shit-done/bin/lib/core.cjs (lines 1184-1187):
```javascript
hasResearch: files.some(f => f.endsWith('-RESEARCH.md') || f === 'RESEARCH.md'),
hasContext: files.some(f => f.endsWith('-CONTEXT.md') || f === 'CONTEXT.md'),
hasVerification: files.some(f => f.endsWith('-VERIFICATION.md') || f === 'VERIFICATION.md'),
hasReviews: files.some(f => f.endsWith('-REVIEWS.md') || f === 'REVIEWS.md'),
```

<!-- init.cjs has_brainstorm locations (grep found these lines with has_context/has_research) -->
From get-shit-done/bin/lib/init.cjs:
```
Line 77-78:   has_research: false, has_context: false,        (default empty object)
Line 235-236: has_research: false, has_context: false,        (default empty object)
Line 273-274: has_research: phaseInfo?.has_research || false,  (phase-op output)
Line 657-658: has_research: false, has_context: false,        (default empty)
Line 707-708: has_research: false, has_context: false,        (default empty)
Line 728-729: has_research: false, has_context: false,        (default empty)
Line 751-752: has_research: phaseInfo?.has_research || false,  (progress-context)
Line 1052-53: has_context: hasContext, has_research: hasResearch, (phase detail)
Line 1264:    has_research: hasResearch,                       (another location)
Line 1290:    has_research: false,                             (fallback)
Line 1598-99: has_context: phaseInfo.has_context, has_research: phaseInfo.has_research, (passthrough)
```

<!-- discuss-phase.md load_prior_context step at lines 204-256 -->
From get-shit-done/workflows/discuss-phase.md (lines 204-256):
Step 1: Read PROJECT.md, REQUIREMENTS.md, STATE.md
Step 2: Read all prior CONTEXT.md files (phase < current)
Step 3: Build <prior_decisions> context
-- Step 2.5 (BRAINSTORM.md) needs to be inserted between Step 2 and Step 3

<!-- discuss-phase-assumptions.md load_prior_context step at lines 138-168 -->
From get-shit-done/workflows/discuss-phase-assumptions.md (lines 138-168):
Same structure: Step 1, Step 2, Step 3
-- Same insertion point

<!-- do.md routing table at line 44 -->
From get-shit-done/workflows/do.md (line 44):
```
| Discussing vision, "how should X look", brainstorming | `/gsd:discuss-phase` | Needs context gathering |
```

<!-- help.md discuss-phase entry at line 63 -->
From get-shit-done/workflows/help.md (lines 61-84):
```
### Phase Planning

**`/gsd:discuss-phase <number>`**
Help articulate your vision for a phase before planning.
...

**`/gsd:research-phase <number>`**
Comprehensive ecosystem research for niche/complex domains.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add hasBrainstorm detection to core.cjs and has_brainstorm to init.cjs</name>
  <files>get-shit-done/bin/lib/core.cjs, get-shit-done/bin/lib/init.cjs</files>
  <read_first>
    get-shit-done/bin/lib/core.cjs
    get-shit-done/bin/lib/init.cjs
  </read_first>
  <action>
**core.cjs patch (per D-02):**

In `getPhaseFileStats()` function (around line 1187), add one line after the `hasReviews` line:

```javascript
hasBrainstorm: files.some(f => f.endsWith('-BRAINSTORM.md') || f === 'BRAINSTORM.md'),
```

Then find where `getPhaseFileStats()` return value is destructured (around line 700):
```javascript
const { plans: unsortedPlans, summaries: unsortedSummaries, hasResearch, hasContext, hasVerification, hasReviews } = getPhaseFileStats(phaseDir);
```
Add `hasBrainstorm` to the destructuring. Then find where those values are assigned to output (around lines 721-724):
```javascript
has_research: hasResearch,
has_context: hasContext,
has_verification: hasVerification,
has_reviews: hasReviews,
```
Add `has_brainstorm: hasBrainstorm,` after `has_reviews`.

**init.cjs patch (per D-02, avoiding Pitfall 1 from RESEARCH.md):**

Grep for EVERY occurrence of `has_research` or `has_context` in init.cjs and add `has_brainstorm` alongside each one. Based on the research, there are ~11 locations:

For lines with `has_research: false, has_context: false` (default empty objects at lines 77, 235, 657, 707, 728, 1290), add:
```javascript
has_brainstorm: false,
```

For lines with `has_research: phaseInfo?.has_research || false` (dynamic lookups at lines 273, 751), add:
```javascript
has_brainstorm: phaseInfo?.has_brainstorm || false,
```

For lines with `has_context: hasContext, has_research: hasResearch` (detail output at line 1052), add:
```javascript
has_brainstorm: hasBrainstorm,
```
And ensure `hasBrainstorm` is extracted from whatever source provides `hasContext`/`hasResearch` in that code block.

For line 1264 (`has_research: hasResearch`), add `has_brainstorm: hasBrainstorm,` -- check the source variable.

For line 1598-1599 (passthrough from phaseInfo), add:
```javascript
has_brainstorm: phaseInfo.has_brainstorm,
```

**CRITICAL: Do NOT miss any location.** After patching, run:
```bash
grep -n "has_research" get-shit-done/bin/lib/init.cjs | wc -l
grep -n "has_brainstorm" get-shit-done/bin/lib/init.cjs | wc -l
```
Both counts should be equal (or has_brainstorm should be >= has_research count).
  </action>
  <verify>
    <automated>grep -q "hasBrainstorm" get-shit-done/bin/lib/core.cjs && RESEARCH_COUNT=$(grep -c "has_research" get-shit-done/bin/lib/init.cjs) && BRAINSTORM_COUNT=$(grep -c "has_brainstorm" get-shit-done/bin/lib/init.cjs) && [ "$BRAINSTORM_COUNT" -ge "$RESEARCH_COUNT" ] && node -e "const c = require('./get-shit-done/bin/lib/core.cjs'); console.log('core OK')" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - core.cjs contains `hasBrainstorm: files.some(f => f.endsWith('-BRAINSTORM.md')` in getPhaseFileStats
    - core.cjs destructuring of getPhaseFileStats includes `hasBrainstorm`
    - core.cjs output object includes `has_brainstorm: hasBrainstorm`
    - init.cjs `has_brainstorm` occurrence count >= `has_research` occurrence count
    - init.cjs line 273 area contains `has_brainstorm: phaseInfo?.has_brainstorm || false`
    - init.cjs line 751 area contains `has_brainstorm: phaseInfo?.has_brainstorm || false`
    - `node -e "require('./get-shit-done/bin/lib/core.cjs')"` exits 0 (no syntax errors)
    - `node -e "require('./get-shit-done/bin/lib/init.cjs')"` exits 0 (no syntax errors)
  </acceptance_criteria>
  <done>getPhaseFileStats() returns hasBrainstorm, all init.cjs code paths expose has_brainstorm, both files load without errors</done>
</task>

<task type="auto">
  <name>Task 2: Patch discuss-phase workflows and update routing</name>
  <files>get-shit-done/workflows/discuss-phase.md, get-shit-done/workflows/discuss-phase-assumptions.md, get-shit-done/workflows/do.md, get-shit-done/workflows/help.md</files>
  <read_first>
    get-shit-done/workflows/discuss-phase.md
    get-shit-done/workflows/discuss-phase-assumptions.md
    get-shit-done/workflows/do.md
    get-shit-done/workflows/help.md
  </read_first>
  <action>
**Patch 1 - discuss-phase.md load_prior_context (per D-03, D-04, avoiding Pitfall 2, 3, 5):**

In `<step name="load_prior_context">` (line 204), insert a new **Step 2.5** between the existing "Step 2: Read all prior CONTEXT.md files" (ends around line 229) and "Step 3: Build internal `<prior_decisions>` context" (starts around line 231):

```markdown
**Step 2.5: Read same-phase BRAINSTORM.md (if exists)**

Check if a brainstorm artifact exists for THIS phase (same-phase only, NOT cross-phase per D-03):

```bash
ls ${phase_dir}/*-BRAINSTORM.md 2>/dev/null || true
```

If BRAINSTORM.md exists for the current phase:
- Read the `<design_decisions>` section -- extract BD-* items
- Read the `<pre_context>` section -- extract assumptions and open questions
- Treat ALL brainstorm decisions as **Likely** confidence (per D-04) -- they are pre-populated assumptions the user can correct, NOT locked decisions
- Store as `<brainstorm_prior>` for use in subsequent steps

**Usage in subsequent steps:**
- `analyze_phase` / `present_gray_areas`: Pre-populate assumptions with brainstorm BD-* decisions. Present as "From brainstorm: [decision]. Keep or change?"
- Gray areas that match brainstorm decisions start pre-answered (user confirms or overrides)
- Open questions from `<pre_context>` become explicit gray areas to discuss

**Key distinction:** Prior CONTEXT.md from earlier phases = locked decisions. Same-phase BRAINSTORM.md = soft assumptions (Likely confidence).
```

**Patch 2 - discuss-phase-assumptions.md load_prior_context (per D-03, D-04, avoiding Pitfall 2):**

In `<step name="load_prior_context">` (line 138), insert the IDENTICAL Step 2.5 block between Step 2 (ends around line 161) and Step 3 (starts around line 163). Use the exact same text as Patch 1 to avoid Pitfall 2 (inconsistent behavior between modes).

Additional usage note for assumptions mode specifically: In the `scout_codebase` and `deep_codebase_analysis` steps, brainstorm BD-* decisions serve as initial hypotheses that the codebase scan can validate or challenge.

**Patch 3 - do.md routing table (per D-13, avoiding Pitfall 4):**

Find line 44 in do.md:
```
| Discussing vision, "how should X look", brainstorming | `/gsd:discuss-phase` | Needs context gathering |
```

SPLIT this into two rows:
```
| Discussing vision, "how should X look" | `/gsd:discuss-phase` | Needs context gathering |
| Brainstorming, exploring ideas, "brainstorm phase" | `/gsd:brainstorm-phase` | Creative exploration before discuss |
```

This keeps "discussing vision" routing to discuss-phase (Pitfall 4 prevention) while routing explicit "brainstorming" to the new command.

**Patch 4 - help.md command listing (per D-14):**

In the `### Phase Planning` section (around line 61), insert BEFORE the `/gsd:discuss-phase` entry:

```markdown
**`/gsd:brainstorm-phase <number>`**
Creative exploration for a phase before discuss-phase.
- Asks one question at a time to explore the design space
- Proposes 2-3 approaches with trade-offs for each decision area
- Produces `{NN}-BRAINSTORM.md` that feeds into discuss-phase as soft assumptions
- Optional pre-step -- skip directly to discuss-phase if you already have clear vision

Usage: `/gsd:brainstorm-phase 3`

```
  </action>
  <verify>
    <automated>grep -q "BRAINSTORM.md" get-shit-done/workflows/discuss-phase.md && grep -q "BRAINSTORM.md" get-shit-done/workflows/discuss-phase-assumptions.md && grep -q "brainstorm-phase" get-shit-done/workflows/do.md && grep -q "brainstorm-phase" get-shit-done/workflows/help.md && grep -q "Discussing vision" get-shit-done/workflows/do.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - discuss-phase.md contains `Step 2.5: Read same-phase BRAINSTORM.md` in load_prior_context
    - discuss-phase.md contains `design_decisions` tag reference
    - discuss-phase.md contains `pre_context` tag reference
    - discuss-phase.md contains `Likely` confidence reference
    - discuss-phase-assumptions.md contains identical Step 2.5 block
    - do.md contains SEPARATE rows: one for "Discussing vision" -> discuss-phase, one for "Brainstorming" -> brainstorm-phase
    - do.md does NOT have "brainstorming" routing to discuss-phase anymore
    - help.md contains `/gsd:brainstorm-phase <number>` entry
    - help.md brainstorm entry appears BEFORE discuss-phase entry (in Phase Planning section)
    - help.md brainstorm entry mentions "BRAINSTORM.md" artifact name
  </acceptance_criteria>
  <done>Both discuss-phase workflows detect same-phase BRAINSTORM.md as Likely-confidence prior context, do.md routes brainstorming to new command while preserving discuss-phase routing for vision discussions, help.md lists the new command</done>
</task>

</tasks>

<verification>
Infrastructure integration complete:
- `node -e "const c = require('./get-shit-done/bin/lib/core.cjs'); console.log('OK')"` exits 0
- `grep -c "has_brainstorm" get-shit-done/bin/lib/init.cjs` returns count >= grep -c "has_research" count
- Both discuss-phase workflows contain BRAINSTORM.md detection
- do.md has separate routing for brainstorming vs discussing vision
- help.md lists brainstorm-phase command
</verification>

<success_criteria>
- getPhaseFileStats() detects BRAINSTORM.md files
- All init.cjs code paths expose has_brainstorm boolean
- discuss-phase (both modes) reads same-phase BRAINSTORM.md as Likely confidence assumptions
- do.md routes "brainstorming" to /gsd:brainstorm-phase without breaking discuss-phase routing
- help.md includes brainstorm-phase in command reference
</success_criteria>

<output>
After completion, create `.planning/phases/04-brainstorm-phase-integration/04-02-SUMMARY.md`
</output>
