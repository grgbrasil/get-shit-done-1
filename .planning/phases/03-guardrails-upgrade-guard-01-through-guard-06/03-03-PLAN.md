---
phase: 03-guardrails-upgrade-guard-01-through-guard-06
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/gsd-executor.md
  - agents/gsd-verifier.md
  - agents/gsd-planner.md
  - agents/gsd-phase-researcher.md
  - agents/gsd-debugger.md
  - tests/agent-context-persistence.test.cjs
autonomous: true
requirements:
  - GUARD-01
  - GUARD-06

must_haves:
  truths:
    - "Executor and verifier agents have anti-false-claims reinforcement"
    - "Planner, researcher, and debugger agents have context_persistence blocks"
    - "Context persistence content is customized per agent role"
  artifacts:
    - path: "agents/gsd-executor.md"
      provides: "Anti-false-claims reinforcement for executor"
      contains: "anti_false_claims"
    - path: "agents/gsd-verifier.md"
      provides: "Anti-false-claims reinforcement for verifier"
      contains: "anti_false_claims"
    - path: "agents/gsd-planner.md"
      provides: "Context persistence for planner"
      contains: "context_persistence"
    - path: "agents/gsd-phase-researcher.md"
      provides: "Context persistence for researcher"
      contains: "context_persistence"
    - path: "agents/gsd-debugger.md"
      provides: "Context persistence for debugger"
      contains: "context_persistence"
    - path: "tests/agent-context-persistence.test.cjs"
      provides: "Validation tests for agent prompt additions"
  key_links:
    - from: "agents/gsd-executor.md"
      to: "/Users/gg/.claude/CLAUDE.md"
      via: "anti_false_claims references global Etica rules"
      pattern: "Regras de Etica.*CLAUDE.md global"
    - from: "agents/gsd-planner.md"
      to: "agents/gsd-executor.md"
      via: "Same context_persistence XML pattern"
      pattern: "<context_persistence>"
---

<objective>
Add anti-false-claims reinforcement to executor and verifier agents (GUARD-01 defense-in-layers) and context_persistence blocks to planner, researcher, and debugger agents (GUARD-06 long-running agent coverage).

Purpose: Defense-in-depth for anti-false-claims (global CLAUDE.md + agent prompts) and context preservation across all long-running agents, not just executor.
Output: 5 updated agent files + validation test.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-guardrails-upgrade-guard-01-through-guard-06/03-CONTEXT.md
@.planning/phases/03-guardrails-upgrade-guard-01-through-guard-06/03-RESEARCH.md

<interfaces>
<!-- Existing context_persistence template from agents/gsd-executor.md (lines 209-221) -->
<context_persistence>
**Write down critical findings before they decay.**

Tool results (Read, Bash, Grep, Glob outputs) may be cleared from context as execution progresses. Before moving to the next task:

1. **Extract key values** -- config paths, function signatures, API responses, error messages
2. **Write to plan notes** -- append to `.planning/phases/{phase-dir}/execution-notes.md` if findings are needed by later tasks
3. **Update STATE.md** -- use `state add-decision` for architectural discoveries that affect future phases

**Rule:** If you read a file and found critical information (a type signature, a config value, an API contract), write it into your current task's commit message or execution notes IMMEDIATELY. Do not assume you can re-read the file later -- context pressure may prevent it.

**Trigger:** After any Bash/Read call that returns data you will need 3+ tasks later, persist it now.
</context_persistence>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add anti-false-claims reinforcement to executor and verifier (GUARD-01)</name>
  <files>agents/gsd-executor.md, agents/gsd-verifier.md</files>
  <read_first>
    - agents/gsd-executor.md (full file — need to find appropriate insertion point, already has context_persistence at line 209)
    - agents/gsd-verifier.md (full file — need to find appropriate insertion point)
    - .planning/phases/03-guardrails-upgrade-guard-01-through-guard-06/03-CONTEXT.md (decisions D-02, D-03)
  </read_first>
  <action>
**GUARD-01 reinforcement (per D-02, D-03):** Add an `<anti_false_claims>` XML section to both agents.

**For agents/gsd-executor.md:** Insert the following block AFTER the existing `<context_persistence>` section (after line 221):

```xml
<anti_false_claims>
**Accuracy over optimism.**

Regras de Etica do CLAUDE.md global se aplicam com forca total aqui:
- Nunca diga "testes passam" quando o output mostra falhas.
- Nunca suprima checks que falham para fabricar resultado verde.
- Nunca caracterize trabalho incompleto como concluido — se ficou stub, diga que ficou stub.
- Antes de reportar `<done>`: rode o verify command, leia o output, confirme que passou.
- Se nao puder verificar (comando falhou, timeout, etc): reporte explicitamente em vez de assumir sucesso.
</anti_false_claims>
```

**For agents/gsd-verifier.md:** Insert the following block early in the agent body (after the opening description/role section, before verification logic):

```xml
<anti_false_claims>
**Report what you observe, not what you hope.**

Como verifier, sua integridade e a ultima linha de defesa:
- Nunca diga "testes passam" quando o output mostra falhas.
- Se um check falhou, reporte FAIL com a evidencia exata (output, erro, arquivo).
- Nunca faca hedge em resultados confirmados — se passou, diga PASS com confianca.
- Se nao conseguiu rodar um check (timeout, dependencia faltando), reporte SKIP com motivo, nunca PASS.
</anti_false_claims>
```
  </action>
  <verify>
    <automated>grep -c "anti_false_claims" agents/gsd-executor.md && grep -c "anti_false_claims" agents/gsd-verifier.md && grep -c "Nunca diga.*testes passam" agents/gsd-executor.md && grep -c "Nunca diga.*testes passam" agents/gsd-verifier.md</automated>
  </verify>
  <acceptance_criteria>
    - agents/gsd-executor.md contains `<anti_false_claims>` opening tag
    - agents/gsd-executor.md contains `</anti_false_claims>` closing tag
    - agents/gsd-executor.md contains "Nunca diga" and "testes passam"
    - agents/gsd-executor.md contains "Regras de Etica do CLAUDE.md global"
    - agents/gsd-verifier.md contains `<anti_false_claims>` opening tag
    - agents/gsd-verifier.md contains `</anti_false_claims>` closing tag
    - agents/gsd-verifier.md contains "Nunca diga" and "testes passam"
    - agents/gsd-verifier.md contains "reporte FAIL com a evidencia exata"
    - agents/gsd-verifier.md contains "reporte SKIP com motivo, nunca PASS"
  </acceptance_criteria>
  <done>Both executor and verifier have anti-false-claims reinforcement blocks referencing global Etica rules. Executor focuses on verify-before-report, verifier focuses on observe-and-report-accurately.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add context_persistence to planner, researcher, debugger (GUARD-06)</name>
  <files>agents/gsd-planner.md, agents/gsd-phase-researcher.md, agents/gsd-debugger.md, tests/agent-context-persistence.test.cjs</files>
  <read_first>
    - agents/gsd-executor.md (lines 209-221 — reference template for context_persistence format)
    - agents/gsd-planner.md (full file — need insertion point)
    - agents/gsd-phase-researcher.md (full file — need insertion point)
    - agents/gsd-debugger.md (full file — need insertion point)
    - .planning/phases/03-guardrails-upgrade-guard-01-through-guard-06/03-CONTEXT.md (decisions D-16, D-17, D-18)
    - .planning/phases/03-guardrails-upgrade-guard-01-through-guard-06/03-RESEARCH.md (pitfall 5: per-agent customization)
  </read_first>
  <behavior>
    - Test: agents/gsd-planner.md contains `<context_persistence>` tag
    - Test: agents/gsd-phase-researcher.md contains `<context_persistence>` tag
    - Test: agents/gsd-debugger.md contains `<context_persistence>` tag
    - Test: agents/gsd-executor.md contains `<context_persistence>` tag (pre-existing, should still be there)
    - Test: Planner context_persistence mentions "architectural decisions" and "dependency chains"
    - Test: Researcher context_persistence mentions "source URLs" and "confidence levels"
    - Test: Debugger context_persistence mentions "root cause" and "reproduction steps"
    - Test: Short agents (gsd-plan-checker.md, gsd-auditor.md) do NOT contain `<context_persistence>` (per D-17)
  </behavior>
  <action>
**Step 1: Create test file** `tests/agent-context-persistence.test.cjs` using `node:test`. The test reads agent files with `fs.readFileSync` and asserts:
- `agents/gsd-planner.md` contains `<context_persistence>`
- `agents/gsd-phase-researcher.md` contains `<context_persistence>`
- `agents/gsd-debugger.md` contains `<context_persistence>`
- `agents/gsd-executor.md` contains `<context_persistence>` (pre-existing)
- `agents/gsd-plan-checker.md` does NOT contain `<context_persistence>` (per D-17)
- Planner file contains "architectural decisions" or "decisoes arquiteturais"
- Researcher file contains "source URLs" or "URLs consultadas"
- Debugger file contains "root cause" or "causa raiz"

**Step 2: Add `<context_persistence>` to agents/gsd-planner.md** — Insert near end of file, before any closing sections. Use same XML structure as executor but customize actions:

```xml
<context_persistence>
**Write down critical findings before they decay.**

Tool results (Read, Bash, Grep, Glob outputs) may be cleared from context during long planning sessions. Before moving to the next plan:

1. **Persist architectural decisions** -- dependency chains, interface contracts, constraint discoveries. Write to plan frontmatter or STATE.md.
2. **Record file signatures** -- when you read a file to understand its exports/types, write the key signatures into the plan's `<interfaces>` block immediately.
3. **Lock requirement mappings** -- once you map a requirement ID to specific tasks, write it to the plan. Don't rely on re-deriving it.

**Rule:** If you discovered a constraint (e.g., "this module uses CommonJS, not ESM") or a dependency ("plan 02 must run before 03 because of shared types"), write it into the plan NOW. Context pressure may prevent re-discovery.

**Trigger:** After any Read/Grep that reveals architecture, types, or constraints you'll reference in later plans, persist it now.
</context_persistence>
```

**Step 3: Add `<context_persistence>` to agents/gsd-phase-researcher.md** — Insert near end of file. Customize for research outputs:

```xml
<context_persistence>
**Write down critical findings before they decay.**

Tool results (Read, Bash, WebFetch, Grep outputs) may be cleared from context during long research sessions. As you research:

1. **Record source URLs and versions** -- library version numbers, API endpoint URLs, documentation links. Write to RESEARCH.md immediately.
2. **Capture verified findings with confidence** -- when you confirm a pattern works or a library supports a feature, write it with confidence level (HIGH/MEDIUM/LOW).
3. **Persist code examples** -- working code snippets from docs or source analysis go into RESEARCH.md Code Examples section, not just your response.

**Rule:** If you found a working code example, a version compatibility fact, or an API contract, write it to RESEARCH.md IMMEDIATELY. Do not assume you can re-fetch the documentation later -- context pressure may prevent it.

**Trigger:** After any WebFetch/Read that returns technical facts you'll reference in recommendations, persist it now.
</context_persistence>
```

**Step 4: Add `<context_persistence>` to agents/gsd-debugger.md** — Insert near end of file. Customize for debug outputs:

```xml
<context_persistence>
**Write down critical findings before they decay.**

Tool results (Read, Bash, Grep outputs) may be cleared from context during long debug sessions. As you investigate:

1. **Record reproduction steps** -- exact commands that trigger the bug, environment state, input data. Write to debug session notes.
2. **Persist root cause hypotheses** -- when you identify a likely cause, write it down with evidence (file, line, output). Don't keep it only in working memory.
3. **Log fix attempts** -- each attempted fix and its result (passed/failed/partial). This prevents re-trying the same approach.

**Rule:** If you found the root cause (a specific line, a race condition, a config mismatch), write it into the debug session notes IMMEDIATELY. Do not assume you can re-read the stack trace later -- context pressure may prevent it.

**Trigger:** After any Bash/Read that reveals error output, stack traces, or behavioral evidence, persist it now.
</context_persistence>
```

**Step 5: Run tests** to verify all additions.
  </action>
  <verify>
    <automated>node --test tests/agent-context-persistence.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - agents/gsd-planner.md contains `<context_persistence>` and `</context_persistence>`
    - agents/gsd-planner.md contains "architectural decisions" or "decisoes arquiteturais"
    - agents/gsd-planner.md contains "dependency chains"
    - agents/gsd-phase-researcher.md contains `<context_persistence>` and `</context_persistence>`
    - agents/gsd-phase-researcher.md contains "source URLs" or "URLs consultadas"
    - agents/gsd-phase-researcher.md contains "confidence" (confidence levels)
    - agents/gsd-debugger.md contains `<context_persistence>` and `</context_persistence>`
    - agents/gsd-debugger.md contains "root cause" or "causa raiz"
    - agents/gsd-debugger.md contains "reproduction steps"
    - agents/gsd-plan-checker.md does NOT contain `<context_persistence>` (per D-17)
    - tests/agent-context-persistence.test.cjs exists and passes with `node --test`
  </acceptance_criteria>
  <done>Three long-running agents have role-specific context_persistence blocks. Planner persists architecture/constraints, researcher persists URLs/findings, debugger persists root cause/reproduction steps. Short agents untouched per D-17.</done>
</task>

</tasks>

<verification>
```bash
# Agent prompt tests pass
node --test tests/agent-context-persistence.test.cjs

# Anti-false-claims present in both agents
grep "anti_false_claims" agents/gsd-executor.md agents/gsd-verifier.md

# Context persistence in all 4 long-running agents
grep "context_persistence" agents/gsd-executor.md agents/gsd-planner.md agents/gsd-phase-researcher.md agents/gsd-debugger.md

# NOT in short agents (per D-17)
grep -L "context_persistence" agents/gsd-plan-checker.md agents/gsd-auditor.md
```
</verification>

<success_criteria>
- Executor and verifier have `<anti_false_claims>` blocks with role-appropriate content
- Planner, researcher, debugger have `<context_persistence>` blocks with role-specific actions
- Short agents (checker, auditor, cataloger, synthesizer) have NO context_persistence (per D-17)
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-guardrails-upgrade-guard-01-through-guard-06/03-03-SUMMARY.md`
</output>
