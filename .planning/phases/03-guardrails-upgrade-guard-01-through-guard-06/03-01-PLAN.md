---
phase: 03-guardrails-upgrade-guard-01-through-guard-06
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/gg/.claude/CLAUDE.md
autonomous: true
requirements:
  - GUARD-01
  - GUARD-02
  - GUARD-05
  - GUARD-06

must_haves:
  truths:
    - "CLAUDE.md global contains expanded anti-false-claims with 4 specific prohibitions"
    - "CLAUDE.md global contains tool result preservation instruction"
    - "CLAUDE.md global contains explicit read-before-edit rule"
    - "CLAUDE.md global contains context compaction preservation instructions"
  artifacts:
    - path: "/Users/gg/.claude/CLAUDE.md"
      provides: "Global guardrail rules for all Claude sessions"
      contains: "Nunca diga todos os testes passam"
  key_links:
    - from: "/Users/gg/.claude/CLAUDE.md"
      to: "All Claude Code sessions"
      via: "Global instruction loading"
      pattern: "Nunca suprima.*checks que falham"
---

<objective>
Add four guardrail text blocks to the global CLAUDE.md: anti-false-claims expansion (GUARD-01), tool result preservation (GUARD-02), read-before-edit explicit rule (GUARD-05 prompt layer), and context compaction instructions (GUARD-06 global layer).

Purpose: Transform single-line generic rules into specific, actionable prohibitions following Claude Code patterns. Defense-in-depth prompt layer.
Output: Updated `/Users/gg/.claude/CLAUDE.md` with 4 new/expanded sections.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-guardrails-upgrade-guard-01-through-guard-06/03-CONTEXT.md
@.planning/phases/03-guardrails-upgrade-guard-01-through-guard-06/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand Etica section and add tool result preservation (GUARD-01, GUARD-02)</name>
  <files>/Users/gg/.claude/CLAUDE.md</files>
  <read_first>
    - /Users/gg/.claude/CLAUDE.md (current state — MUST see existing Etica section and overall structure)
    - .planning/phases/03-guardrails-upgrade-guard-01-through-guard-06/03-RESEARCH.md (code examples section for exact text)
    - .planning/phases/03-guardrails-upgrade-guard-01-through-guard-06/03-CONTEXT.md (decisions D-01 through D-06)
  </read_first>
  <action>
**GUARD-01 (per D-01, D-03):** Replace the existing `## Etica` section content with the expanded version. The current section has 2 lines:
```
## Etica
- Nunca afirme que fez algo que nao fez. Nunca afirme que resolveu se nao resolveu.
```

Replace with this exact content:
```
## Etica
- Nunca afirme que fez algo que nao fez. Nunca afirme que resolveu se nao resolveu.
- Nunca diga "todos os testes passam" quando o output mostra falhas.
- Nunca suprima ou simplifique checks que falham para fabricar resultado verde.
- Nunca caracterize trabalho incompleto ou quebrado como concluido.
- Na direcao oposta: quando um check passou, diga claramente — nao faca hedge em resultados confirmados com disclaimers desnecessarios.
- Antes de reportar conclusao: rode o teste, execute o script, verifique o output. Se nao puder verificar, diga explicitamente em vez de assumir sucesso.
```

**GUARD-02 (per D-04, D-05, D-06):** Add a new subsection `## Preservacao de Resultados` immediately after the `## Etica` section, with this exact content:
```
## Preservacao de Resultados
- Anote informacoes importantes na sua resposta — tool results (Read, Bash, Grep, Glob) podem ser limpos do contexto depois.
- Foco em: file paths, error messages, code snippets, decisoes tomadas.
- Se leu um arquivo e encontrou informacao critica (assinatura de funcao, valor de config, contrato de API), escreva na resposta IMEDIATAMENTE. Nao assuma que pode re-ler depois.
```
  </action>
  <verify>
    <automated>grep -c "Nunca diga.*todos os testes passam" /Users/gg/.claude/CLAUDE.md && grep -c "Preservacao de Resultados" /Users/gg/.claude/CLAUDE.md && grep -c "tool results.*Read.*Bash.*Grep.*Glob" /Users/gg/.claude/CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - /Users/gg/.claude/CLAUDE.md contains "Nunca diga" followed by "todos os testes passam"
    - /Users/gg/.claude/CLAUDE.md contains "Nunca suprima ou simplifique checks que falham"
    - /Users/gg/.claude/CLAUDE.md contains "Nunca caracterize trabalho incompleto"
    - /Users/gg/.claude/CLAUDE.md contains "nao faca hedge em resultados confirmados"
    - /Users/gg/.claude/CLAUDE.md contains "## Preservacao de Resultados"
    - /Users/gg/.claude/CLAUDE.md contains "tool results (Read, Bash, Grep, Glob) podem ser limpos"
    - /Users/gg/.claude/CLAUDE.md contains "file paths, error messages, code snippets, decisoes tomadas"
    - Etica section has exactly 6 bullet points (original + 5 new)
  </acceptance_criteria>
  <done>Etica section expanded with 4 bidirectional anti-false-claims prohibitions. Tool result preservation section added with focus on file paths, errors, snippets, decisions.</done>
</task>

<task type="auto">
  <name>Task 2: Add read-before-edit rule and compaction instructions (GUARD-05 text, GUARD-06 global)</name>
  <files>/Users/gg/.claude/CLAUDE.md</files>
  <read_first>
    - /Users/gg/.claude/CLAUDE.md (must see current state after Task 1 edits)
    - .planning/phases/03-guardrails-upgrade-guard-01-through-guard-06/03-CONTEXT.md (decisions D-11, D-15, D-16)
  </read_first>
  <action>
**GUARD-05 text layer (per D-11):** Find the existing `## Integridade do Sistema` section. Add this bullet at the END of the existing bullets in that section:
```
- SEMPRE leia o arquivo antes de editar. Nunca edite baseado em memoria ou suposicoes sobre conteudo atual — use Read primeiro, depois Edit/Write.
```

**GUARD-06 global layer (per D-15):** Add a new section `## Compaction e Contexto` at the END of the `## Regras de Operacao SIJUR` block (before `## Architecture & Cross-Phase`), with this exact content:
```
## Compaction e Contexto
- Quando contexto for compactado mid-session, preserve obrigatoriamente: file paths exatos, error messages completos, code snippets relevantes, pending tasks, decisoes tomadas.
- Nunca resuma file paths como "os arquivos relevantes" — mantenha o caminho completo.
- Nunca resuma erros como "houve erros" — mantenha a mensagem de erro exata.
- Se perdeu contexto de uma operacao anterior, re-leia o arquivo ou re-rode o comando antes de prosseguir.
```
  </action>
  <verify>
    <automated>grep -c "SEMPRE leia o arquivo antes de editar" /Users/gg/.claude/CLAUDE.md && grep -c "Compaction e Contexto" /Users/gg/.claude/CLAUDE.md && grep -c "Nunca resuma file paths" /Users/gg/.claude/CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - /Users/gg/.claude/CLAUDE.md Integridade do Sistema section contains "SEMPRE leia o arquivo antes de editar"
    - /Users/gg/.claude/CLAUDE.md contains "## Compaction e Contexto"
    - /Users/gg/.claude/CLAUDE.md contains "file paths exatos, error messages completos, code snippets relevantes"
    - /Users/gg/.claude/CLAUDE.md contains "Nunca resuma file paths como"
    - /Users/gg/.claude/CLAUDE.md contains "Nunca resuma erros como"
    - Section appears before "## Architecture & Cross-Phase" in file order
  </acceptance_criteria>
  <done>Read-before-edit explicit rule added to Integridade section. Compaction preservation instructions added as standalone section with specific anti-pattern prohibitions.</done>
</task>

</tasks>

<verification>
All four GUARD text requirements verified via grep on /Users/gg/.claude/CLAUDE.md:
- GUARD-01: "Nunca diga.*todos os testes passam" present
- GUARD-02: "Preservacao de Resultados" section present
- GUARD-05: "SEMPRE leia o arquivo antes de editar" present
- GUARD-06: "Compaction e Contexto" section present
</verification>

<success_criteria>
- CLAUDE.md global Etica section has 6 bullet points (was 1)
- CLAUDE.md global has "Preservacao de Resultados" section
- CLAUDE.md global Integridade section has read-before-edit rule
- CLAUDE.md global has "Compaction e Contexto" section before Architecture section
- All text in Portuguese with technical terms in English (matching existing style)
</success_criteria>

<output>
After completion, create `.planning/phases/03-guardrails-upgrade-guard-01-through-guard-06/03-01-SUMMARY.md`
</output>
